<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cu.jesusd0897.farmakit">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme.Dark"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activity.SplashActivity"
            android:configChanges="screenSize|orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:configChanges="screenSize|orientation"
            android:label="@string/app_name" />
        <activity android:name=".activity.LibrariesActivity" />
        <activity android:name=".activity.ChangelogActivity" />
        <activity android:name=".activity.SettingsActivity" />
        <activity android:name=".activity.AboutActivity" />
        <activity android:name=".activity.NaturalProductDetailActivity" />
        <activity android:name=".activity.ProductDetailActivity" />
        <activity android:name=".activity.ScannerActivity" />
        <activity android:name=".activity.ToolsActivity" />
        <activity
            android:name=".activity.ToolDetailActivity"
            android:configChanges="screenSize|orientation" />
        <activity android:name=".activity.RisksActivity" />
        <activity android:name=".activity.RiskDetailActivity" />
    </application>

</manifest>